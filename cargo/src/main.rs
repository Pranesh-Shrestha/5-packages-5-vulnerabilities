// Stub entry point. Present only so Cargo accepts the manifest for
// `cargo generate-lockfile` / `cargo fetch`. The 5 vulnerable crates are
// declared in Cargo.toml; nothing here is built in CI.
fn main() {}
